const r=require('express').Router(),c=require('../controllers/orderController'),{protect,adminOnly}=require('../middleware/auth');r.post('/',protect,c.place);r.get('/my',protect,c.my);r.get('/',protect,adminOnly,c.all);r.get('/:id',protect,c.byId);r.patch('/:id/status',protect,adminOnly,c.status);r.patch('/:id/assign',protect,adminOnly,c.assign);r.patch('/:id/cancel',protect,c.cancel);module.exports=r;
