const r=require('express').Router(),c=require('../controllers/orderController'),{protect,deliveryOnly}=require('../middleware/auth');r.get('/orders',protect,deliveryOnly,c.deliveryOrders);r.patch('/orders/:id/status',protect,deliveryOnly,c.deliveryStatus);r.post('/orders/:id/complete',protect,deliveryOnly,c.complete);r.post('/orders/:id/cancel',protect,deliveryOnly,c.deliveryCancel);module.exports=r;
